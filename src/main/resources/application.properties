# Spring Profiles
spring.profiles.active=dev


# Cloudinary Configuration
cloudinary.cloud-name=ddgrqyqf6
cloudinary.api-key=424292915993457
cloudinary.api-secret=zABo3IX4MJH78kmiOOfRqAYXTVs
cloudinary.secure=true

# File upload settings
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Default Server Configuration
server.servlet.context-path=/api
server.error.include-stacktrace=never
server.error.include-message=always
server.compression.enabled=true
server.compression.mime-types=text/html,text/xml,text/plain,text/css,text/javascript,application/javascript,application/json

# Spring Boot Configuration
spring.main.banner-mode=off
spring.main.allow-bean-definition-overriding=true

# Jackson Configuration
spring.jackson.date-format=yyyy-MM-dd HH:mm:ss
spring.jackson.time-zone=UTC
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.serialization.fail-on-empty-beans=false

# File Upload
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# Validation
spring.mvc.throw-exception-if-no-handler-found=true

# Actuator
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always


# Enable Spring MVC logging
logging.level.org.springframework.web.servlet.mvc=DEBUG
logging.level.org.springframework.web.servlet.handler=DEBUG
logging.level.com.mockperiod.main.controller=DEBUG

# Show all mapped endpoints on startup
logging.level.org.springframework.boot.autoconfigure=DEBUG


allowed.origins=http://localhost:3000


# Essential multipart settings
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Server settings to match
server.tomcat.max-swallow-size=10MB

# HikariCP Configuration
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.max-lifetime=1200000
spring.datasource.hikari.leak-detection-threshold=60000

# Logging to debug connection issues
logging.level.com.zaxxer.hikari=DEBUG

